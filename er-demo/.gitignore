# Dependencies
node_modules

# Build output (Vite/React — no `build`/`.svelte-kit`, this isn't SvelteKit)
dist
dist-ssr
*.local

# Environment files
.env
.env.*
!.env.example

# Package manager — no lockfile committed, matching this repo's root .gitignore convention
# (package-lock.json is already excluded there for the same reason).
bun.lock
bun.lockb
package-lock.json
pnpm-lock.yaml

# Testing
coverage

# IDE
.idea
.vscode/*
!.vscode/extensions.json
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Nix/direnv
.direnv

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
